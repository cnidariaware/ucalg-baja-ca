name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - master
      - dev
      - "**"
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build SvelteKit
        run: npm run build

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Deploy to Cloudflare Pages
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BRANCH_NAME="pr-${{ github.event.number }}"
          else
            BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          fi

          echo "Deploying branch: $BRANCH_NAME"

          wrangler pages deploy .svelte-kit/cloudflare \
            --project-name ucalg-baja-ca \
            --branch "$BRANCH_NAME" \
            --commit-hash "${GITHUB_SHA}" \
            --commit-message "${GITHUB_COMMIT_MESSAGE}" \
            --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
<<<<<<< HEAD

      - name: Send formatted webhook
        if: always()
        run: |
          short_sha=$(echo "$GITHUB_SHA" | cut -c1-7)
          short_sha_preview=$(echo "$GITHUB_SHA" | cut -c1-8)
          branch="$GITHUB_REF_NAME"
          repo="$GITHUB_REPOSITORY"
          actor="$GITHUB_ACTOR"

          commit_message=$(printf '%s' "$GITHUB_COMMIT_MESSAGE" | tr '\n' ' ' | sed -E 's/\\/\\\\/g; s/"/\\"/g' | cut -c1-200)

          commit_url="https://forge.ucalgarybaja.ca/${repo}/commit/${GITHUB_SHA}"
          sha_preview_url="https://${short_sha_preview}.ucalg-baja-ca.pages.dev"
          branch_preview_url="https://${branch}.ucalg-baja-ca.pages.dev"

          if [ "${PREV_STEP_EXIT_CODE:-0}" -eq 0 ]; then
            color=3066993
            status_emoji="✅"
            status_text="Deploy successful!"
          else
            color=15158332
            status_emoji="❌"
            status_text="Deploy failed!"
          fi

          payload=$(jq -n \
            --arg title "[$repo:$branch] Deployment status" \
            --arg title_url "$commit_url" \
            --arg latest_commit "$short_sha" \
            --arg sha_preview_url "$sha_preview_url" \
            --arg branch_preview_url "$branch_preview_url" \
            --arg status_emoji "$status_emoji" \
            --arg status_text "$status_text" \
            --arg footer "$actor • $commit_message" \
            --argjson color "$color" \
            '{
              embeds: [
                {
                  title: $title,
                  url: $title_url,
                  color: $color,
                  description: ("Latest commit: \($latest_commit) <\($title_url)>\n\($status_emoji) \($status_text)\nSHA preview URL: <\($sha_preview_url)>\nBranch preview URL: <\($branch_preview_url)>"),
                  footer: { text: $footer }
                }
              ]
            }'
          )

          # --- send to webhooks safely ---
          IFS=$'\n'
          for url in $WEBHOOK_URLS; do
            url_trimmed=$(echo "$url" | xargs)
            if [ -n "$url_trimmed" ]; then
              echo "→ Sending formatted embed to Discord: $url_trimmed"
              curl -s -o /dev/null -w "%{http_code}\n" \
                   -H "Content-Type: application/json" \
                   -d "$payload" "$url_trimmed"
            fi
          done
        env:
          WEBHOOK_URLS: |
            ${{ secrets.DARKICEWOLF50_WEBHOOK }}
            ${{ secrets.BAJA_WEBHOOK }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
=======
      
      - name: Send formatted webhook
        run: |
          payload="{
          \"content\": \"${GITHUB_ACTOR}\n[${GITHUB_REPOSITORY}:${GITHUB_REF_NAME}] 1 new commit\n${GITHUB_SHA::7} ${GITHUB_COMMIT_MESSAGE} - ${GITHUB_ACTOR}\n\n✅ Action succeeded in ${GITHUB_REPOSITORY} ${GITHUB_REF_NAME}\"
          }"

          for url in $WEBHOOK_URLS; do
            curl -s -X POST -H "Content-Type: application/json" \
              -d "$payload" "$url"
          done
        env:
          WEBHOOK_URLS: >-
            ${{ secrets.DARKICEWOLF50_WEBHOOK }}
            ${{ secrets.BAJA_WEBHOOK }}
          GITHUB_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          
          echo "::notice::$AUTHOR"
          echo "::notice@deploy::$REPO:$BRANCH 1 new commit"
          echo "::notice::$SHORT_SHA $COMMIT_MSG - $AUTHOR"
          echo "::notice@deploy::$REPO Deployed successfully on $BRANCH"
          echo "::notice@deploy::Latest commit: $SHORT_SHA"
          echo "::notice@deploy::Status: ✅ Deploy successful!"
          echo "::notice@deploy::Preview URL: https://${SHORT_SHA}.uofcbaja.pages.dev/"
          echo "::notice@deploy::[Cloudflare Workers and Pages checks success on $BRANCH]"
>>>>>>> 032fed7 (feat(actions): more detailed webhook notification)
